---
- name: System Initialization
  hosts: "{{ host | default('localhost') }}"
  become: true
  gather_facts: true
  vars:
    upgrade: false
    sysinit_var: system_initialization
    install_reach: true
  roles:
    - sysinit
    - role: reach
      when: install_reach | bool
